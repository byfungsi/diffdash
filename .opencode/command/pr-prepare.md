---
description: Audit the current branch, prepare its desktop Changeset, and run PR readiness checks without committing or pushing.
agent: build
---

Load the `pr-prepare` skill and prepare the current DiffDash branch for a pull request.

Arguments from the user:

```text
$ARGUMENTS
```

Follow the skill exactly. Never commit, push, force-push, open a pull request, or rewrite history without the approval required by the skill.
