{
  "name": "@diffdash/app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "exports": {
    "./file-tree-adapter": "./src/file-tree-adapter.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "oxlint src --no-error-on-unmatched-pattern"
  },
  "dependencies": {
    "@diffdash/domain": "workspace:*",
    "@pierre/trees": "1.0.0-beta.5"
  },
  "devDependencies": {
    "@effect/vitest": "catalog:",
    "oxlint": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
