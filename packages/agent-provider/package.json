{
  "name": "@diffdash/agent-provider",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "exports": {
    ".": "./src/agent-provider.ts",
    "./registry": "./src/registry.ts",
    "./testing": "./src/testing.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "oxlint src --no-error-on-unmatched-pattern"
  },
  "dependencies": {
    "effect": "catalog:"
  },
  "devDependencies": {
    "@effect/vitest": "catalog:",
    "oxlint": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
